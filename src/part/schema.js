// Superseded by note.js + part.js + part-set.js (prior session's implementation).
// Kept as an empty file so references in build cache don't break.
// Import from those files directly, or from the index.
